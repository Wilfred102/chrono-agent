const ethers = require('ethers');

async function startIndexer() {
    console.log("Starting indexer...");
    // Connect to provider
    // Listen for InvoiceCreated, PaymentReleased events
    // Update DB
}

module.exports = { startIndexer };
